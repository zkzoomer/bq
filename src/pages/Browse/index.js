import styled from "styled-components"

const BroseWrapper = styled.div`
    width: 100%;
    background-color: var(--main-background);
    height: 200vh;
`

export function Browse () {
    return (
        <BroseWrapper>
            browse section
        </BroseWrapper>
    )
}